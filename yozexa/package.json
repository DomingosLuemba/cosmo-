{
  "name": "yozexa-monorepo",
  "private": true,
  "version": "0.1.0",
  "license": "Apache-2.0",
  "workspaces": [
    "sdk/typescript",
    "indexer",
    "pay",
    "explorer",
    "wallet/web",
    "business",
    "developer"
  ],
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run typecheck"
  }
}
