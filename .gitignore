# Node / backend
node_modules/
dist/
.env
*.log

# macOS
.DS_Store

# Xcode (quando o projeto .xcodeproj for criado neste repo)
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/
*.xcuserstate

# TypeScript incremental build cache. Machine-local, and checking it in makes
# every build show up as a change.
*.tsbuildinfo

# Next.js build output
.next/
